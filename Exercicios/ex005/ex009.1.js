var idade = 16

if (idade > 18) {
    console.log('Maior de idade votante!!')
} else {
    if (idade >= 16) {
        console.log('Menor de Idade votante!!')
    } else {
        console.log('Menor de idade que não vota!!')
    }
}